services:
  app:
    build: .
    container_name: leakybucket-app
    restart: unless-stopped
    env_file: .env
    environment:
      NODE_ENV: production
      PORT: 4000
    ports:
      - "4000:4000"
